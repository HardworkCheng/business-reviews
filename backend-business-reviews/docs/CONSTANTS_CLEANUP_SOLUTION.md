# 常量类清理与规范方案

> 本文档详细说明了 Business Reviews 系统中重复常量类的清理方案，建立统一的常量管理规范。

## 📋 目录

1. [问题背景](#问题背景)
2. [技术分析](#技术分析)
3. [解决方案](#解决方案)
4. [常量类设计规范](#常量类设计规范)
5. [使用指南](#使用指南)
6. [最佳实践](#最佳实践)
7. [清理清单](#清理清单)
8. [常见问题](#常见问题)

---

## 问题背景

### 常量管理混乱的问题

在项目演进过程中，常量的定义逐渐散落在多个位置，导致管理混乱：

```
┌─────────────────────────────────────────────────────────────────────┐
│                    重构前：常量定义混乱                               │
└─────────────────────────────────────────────────────────────────────┘

com.businessreviews.common.Constants          ◀─── 废弃的空壳类
         │
         ├── 已被拆分到：
         │
         ▼
com.businessreviews.constants.RedisKeyConstants   ◀─── Redis Key 前缀
com.businessreviews.constants.SmsCodeConstants    ◀─── 短信验证码配置
com.businessreviews.constants.CacheExpireConstants◀─── 缓存过期时间
com.businessreviews.constants.PageConstants       ◀─── 分页默认值
com.businessreviews.constants.FileUploadConstants ◀─── 文件上传限制

问题：
1. 废弃的 Constants 类仍然存在，造成困惑
2. 部分常量有重复定义（如 SMS_CODE 和 SMS_CODE_PREFIX）
3. 新人不知道该使用哪个
```

### 典型问题场景

```java
// 问题 1：废弃常量仍被引用
String limitKey = RedisKeyConstants.SMS_LIMIT_PREFIX + phone;  // ❌ 废弃的

// 问题 2：同一个值有两个名字
// RedisKeyConstants.java 中：
public static final String SMS_CODE = "sms:code:";         // 新的
public static final String SMS_CODE_PREFIX = "sms:code:";  // 废弃的（@deprecated）

// 问题 3：新人不知道用哪个
// 有的地方用 SMS_CODE，有的地方用 SMS_CODE_PREFIX，造成代码不一致
```

### 问题影响

| 影响范围 | 描述 |
|----------|------|
| 🔴 **代码混乱** | 同一常量多种写法，代码不一致 |
| 🔴 **维护困难** | 修改时需要同步多个位置 |
| 🟡 **新人困惑** | 不知道该用哪个常量类 |
| 🟡 **废弃代码** | 无用的类和常量占用空间 |

---

## 技术分析

### 常量清理前状态

| 类名 | 位置 | 状态 | 说明 |
|------|------|------|------|
| `Constants` | `common` | ⚠️ 废弃 | 空壳类，只有注释 |
| `RedisKeyConstants` | `constants` | ✅ 活跃 | 含废弃字段 `*_PREFIX` |
| `SmsCodeConstants` | `constants` | ✅ 活跃 | 含废弃字段 `CODE_EXPIRE_SECONDS` |
| `CacheExpireConstants` | `constants` | ✅ 活跃 | 正常 |
| `PageConstants` | `constants` | ✅ 活跃 | 正常 |
| `FileUploadConstants` | `constants` | ✅ 活跃 | 正常 |

### 废弃常量使用情况

```
┌─────────────────────────────────────────────────────────────────────┐
│                    废弃常量引用分析                                   │
└─────────────────────────────────────────────────────────────────────┘

RedisKeyConstants.SMS_CODE_PREFIX
    └── 被 SmsManager.java 引用（4处）

RedisKeyConstants.SMS_LIMIT_PREFIX
    └── 被 SmsManager.java 引用（1处）

SmsCodeConstants.CODE_EXPIRE_SECONDS
    └── 被 SmsManager.java 引用（1处）
```

---

## 解决方案

### 清理策略

```
┌─────────────────────────────────────────────────────────────────────┐
│                        清理策略                                      │
└─────────────────────────────────────────────────────────────────────┘

步骤 1：更新引用代码
─────────────────────
将所有使用废弃常量的代码改为使用新常量：
  SMS_CODE_PREFIX → SMS_CODE
  SMS_LIMIT_PREFIX → SMS_LIMIT
  CODE_EXPIRE_SECONDS → EXPIRE_TIME

步骤 2：删除废弃常量
─────────────────────
从常量类中移除带 @deprecated 标记的常量

步骤 3：删除废弃类
─────────────────────
删除 com.businessreviews.common.Constants 空壳类
```

### 最终常量结构

```
com.businessreviews.constants/
├── RedisKeyConstants.java     # Redis Key 前缀
│   ├── SMS_CODE              # "sms:code:"
│   ├── SMS_LIMIT             # "sms:limit:"
│   ├── MERCHANT_SMS_CODE     # "merchant:sms:code:"
│   ├── USER_INFO             # "user:info:"
│   └── ...
│
├── SmsCodeConstants.java      # 短信验证码配置
│   ├── CODE_LENGTH           # 6
│   ├── EXPIRE_TIME           # 300（秒）
│   ├── LIMIT_TIME            # 60（秒）
│   └── SEND_INTERVAL_SECONDS # 60（秒）
│
├── CacheExpireConstants.java  # 缓存过期时间
│   ├── SMS_CODE              # 300
│   ├── SMS_LIMIT             # 10
│   ├── USER_INFO             # 1800
│   └── ...
│
├── PageConstants.java         # 分页默认值
│   ├── DEFAULT_PAGE_NUM      # 1
│   ├── DEFAULT_PAGE_SIZE     # 10
│   └── MAX_PAGE_SIZE         # 50
│
└── FileUploadConstants.java   # 文件上传限制
    ├── ALLOWED_IMAGE_EXTENSIONS
    ├── MAX_FILE_SIZE
    └── ...
```

---

## 常量类设计规范

### 标准常量类模板

```java
package com.businessreviews.constants;

/**
 * Redis Key 键名常量类
 * <p>
 * 统一管理系统中使用的 Redis Key 前缀，避免 Key 冲突和拼写错误。
 * 命名规范：业务模块:功能:子项:
 * </p>
 */
public final class RedisKeyConstants {

    /**
     * 私有构造函数，防止实例化
     */
    private RedisKeyConstants() {
        // 工具类不允许实例化
    }

    /** 短信验证码 Key 前缀（后面拼接手机号） */
    public static final String SMS_CODE = "sms:code:";

    /** 短信发送频率限制 Key 前缀 */
    public static final String SMS_LIMIT = "sms:limit:";

    // ... 更多常量
}
```

### 设计原则

| 原则 | 说明 |
|------|------|
| **final 类** | 使用 `public final class` 防止继承 |
| **私有构造** | 私有构造函数防止实例化 |
| **static final** | 所有常量都是 `public static final` |
| **清晰命名** | 常量名应自解释，如 `SMS_CODE` 而非 `SC` |
| **JavaDoc 注释** | 每个常量都应有注释说明用途 |
| **按职责拆分** | 不同用途的常量放在不同类中 |

### 命名规范

| 类型 | 命名格式 | 示例 |
|------|----------|------|
| Redis Key | `业务_功能` | `USER_INFO`、`SMS_CODE` |
| 时间（秒） | `XXX_TIME` 或 `XXX_SECONDS` | `EXPIRE_TIME` |
| 数量 | `XXX_COUNT` 或 `XXX_SIZE` | `MAX_PAGE_SIZE` |
| 限制 | `XXX_LIMIT` 或 `MAX_XXX` | `LIMIT_TIME` |

---

## 使用指南

### Redis Key 使用

```java
// 构建 Redis Key
String codeKey = RedisKeyConstants.SMS_CODE + phone;
String limitKey = RedisKeyConstants.SMS_LIMIT + phone;
String userKey = RedisKeyConstants.USER_INFO + userId;

// 使用示例
redisUtil.set(codeKey, code, SmsCodeConstants.EXPIRE_TIME);
```

### 缓存过期时间使用

```java
// 使用 CacheExpireConstants 中的预定义过期时间
redisUtil.set(key, value, CacheExpireConstants.USER_INFO);  // 30分钟
redisUtil.set(key, value, CacheExpireConstants.SHOP_INFO);  // 1小时
```

### 分页参数使用

```java
// 使用 PageConstants 进行分页校验
public PageResult<T> queryList(int pageNum, int pageSize) {
    // 参数校验
    if (pageNum < 1) {
        pageNum = PageConstants.DEFAULT_PAGE_NUM;
    }
    if (pageSize < 1 || pageSize > PageConstants.MAX_PAGE_SIZE) {
        pageSize = PageConstants.DEFAULT_PAGE_SIZE;
    }
    // ...
}
```

---

## 最佳实践

### ✅ 推荐做法

```java
// 1. 使用标准常量名
String key = RedisKeyConstants.SMS_CODE + phone;  // ✅

// 2. 常量组合使用
String key = RedisKeyConstants.USER_INFO + userId;
redisUtil.setObject(key, user, CacheExpireConstants.USER_INFO);

// 3. 在枚举中引用常量
public enum CacheKey {
    USER_INFO(RedisKeyConstants.USER_INFO, CacheExpireConstants.USER_INFO);
    // ...
}

// 4. 为新模块创建专用常量类
// 如果常量较多，考虑创建 XxxConstants.java
```

### ❌ 避免的做法

```java
// 1. 不要使用废弃的常量名
String key = RedisKeyConstants.SMS_CODE_PREFIX + phone;  // ❌ 已删除

// 2. 不要在代码中硬编码 Key 前缀
String key = "sms:code:" + phone;  // ❌ 应使用常量

// 3. 不要定义局部常量覆盖全局常量
private static final String SMS_CODE = "sms:code:";  // ❌ 重复定义

// 4. 不要在多个类中定义相同的常量
// 应该集中到 xxxConstants.java 中
```

---

## 清理清单

### 已删除文件

| 文件 | 原位置 | 原因 |
|------|--------|------|
| `Constants.java` | `com.businessreviews.common` | 废弃空壳类 |

### 已删除常量

| 常量 | 原所在类 | 替代常量 |
|------|----------|----------|
| `SMS_CODE_PREFIX` | `RedisKeyConstants` | `SMS_CODE` |
| `SMS_LIMIT_PREFIX` | `RedisKeyConstants` | `SMS_LIMIT` |
| `CODE_EXPIRE_SECONDS` | `SmsCodeConstants` | `EXPIRE_TIME` |

### 已更新引用

| 文件 | 修改内容 |
|------|----------|
| `SmsManager.java` | 将所有废弃常量引用改为新常量 |

---

## 常见问题

### 1. 何时应该创建新的常量类？

```
判断标准：
- 常量数量 >= 5 个
- 常量属于独立的业务模块
- 常量可能被多个类共用

示例：
- 支付模块常量 → PaymentConstants.java
- 文件上传常量 → FileUploadConstants.java
```

### 2. 常量应该放在哪个模块？

```
┌─────────────────────────────────────────────────────────────────────┐
│                       常量类位置规则                                  │
└─────────────────────────────────────────────────────────────────────┘

通用常量（任何模块都可能用）：
    → backend-business-reviews-common/src/.../constants/

业务特定常量（只有某个模块使用）：
    → 放在对应模块的 constants 包下

枚举常量（状态、类型等）：
    → backend-business-reviews-common/src/.../enums/
```

### 3. 如何处理已弃用的常量？

```java
// 步骤 1：标记为 @deprecated（给出过渡期）
/** @deprecated 请使用 SMS_CODE */
@Deprecated
public static final String SMS_CODE_PREFIX = "sms:code:";

// 步骤 2：在下个版本中删除
// 确保没有代码引用后，删除该常量
```

### 4. 常量值需要修改怎么办？

```java
// 常量值修改后，所有引用处自动生效
// 这正是使用常量的好处：一处修改，全局生效

// 例如：调整 SMS 发送间隔
public static final long SEND_INTERVAL_SECONDS = 60;  // 改为 30
```

---

## 总结

通过本次清理，我们建立了清晰的常量管理规范：

| 目标 | 实现效果 |
|------|----------|
| 消除重复 | 删除废弃的 Constants 类和重复常量 |
| 统一规范 | 建立标准的常量类设计模板 |
| 清晰分类 | 按职责将常量分类到不同常量类 |
| 便于维护 | 修改常量值只需改一处 |
| 减少困惑 | 新人可以清楚知道使用哪个常量 |

### 当前常量类清单

| 常量类 | 职责 |
|--------|------|
| `RedisKeyConstants` | Redis Key 前缀定义 |
| `SmsCodeConstants` | 短信验证码配置 |
| `CacheExpireConstants` | 缓存过期时间配置 |
| `PageConstants` | 分页参数默认值 |
| `FileUploadConstants` | 文件上传限制 |

这套规范确保了常量管理的清晰和一致，降低了代码维护成本。
