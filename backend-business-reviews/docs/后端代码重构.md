步骤一：重构项目分层结构（最优先推荐）

[cite_start]请你扮演一位精通《阿里巴巴Java开发手册》的架构师，帮我重构当前 Spring Boot 项目的工程结构。请严格遵循手册中“应用分层”的规范 [cite: 776-794]，按以下要求重新组织代码包结构并移动现有类：

1. **Web层 (`controller`包)**：
   - 职责：只负责参数校验、异常捕获和简单的业务转发。
   - 约束：**禁止**包含核心业务逻辑。
   - 返回值：所有接口必须通过统一的 `Result<T>` 对象包装返回（包含 code, message, data）。

2. **Service层 (`service`包)**：
   - 职责：核心业务逻辑处理。
   - [cite_start]结构：严格遵循 SOA 理念，必须定义接口（`XxxService`），实现类放在 `impl` 子包下（`XxxServiceImpl`）[cite: 56]。
   - 约束：上层 Web 层只能调用 Service 接口，不能直接依赖实现类。

3. **DAO层 (`dao` 或 `mapper`包)**：
   - 职责：数据持久层，只与数据库交互。
   - 约束：返回的对象应该是 DO（与数据库表一一对应）。

4. **Manager层 (`manager`包 - 可选)**：
   - [cite_start]如果发现我的 Service 层逻辑过重，或者有第三方接口调用（如短信、OSS），请将这部分逻辑剥离到 Manager 层 [cite: 789-792]。

请分析我当前的项目结构，并给出重构后的目录结构建议和移动计划。



步骤二:拆分领域模型 (POJO)

请根据《阿里巴巴Java开发手册》中关于“领域模型规约”的要求 [cite: 798-804]，对我项目中的实体类（Entity/POJO）进行拆分和重构。目前的结构不合理，请按以下标准生成对应的类：

1. **DO (Data Object)**：
   - 位置：`model.do`
   - 规范：与数据库表结构一一对应。
   - 命名：以 `DO` 结尾（例如 `UserDO`）。

2. **DTO (Data Transfer Object)**：
   - 位置：`model.dto`
   - 规范：用于 Service 层向外传输数据，或者 Web 层接收前端传入的复杂表单。
   - 命名：以 `DTO` 结尾（例如 `UserDTO`）。

3. **VO (View Object)**：
   - 位置：`model.vo`
   - 规范：专门用于 Web 层返回给前端展示。**必须脱敏**（例如隐藏密码、手机号中间四位）。
   - 命名：以 `VO` 结尾（例如 `UserVO`）。

4. **Query (查询对象)**：
   - [cite_start]规范：如果 Controller 接收的查询参数超过 2 个，请不要用 Map，而是封装成 Query 对象 [cite: 804]。

请帮我生成这些类，并在 Service 层或 Controller 层使用 BeanUtils 或 MapStruct 添加对象转换逻辑。

步骤三：规范命名与代码细节

请检查我的代码，并严格按照《阿里巴巴Java开发手册》的“编程规约”进行修正，重点关注以下细节：

1. [cite_start]**方法命名修正** [cite: 65-71]：
   - 获取单个对象的方法用 `get` 做前缀。
   - 获取多个对象用 `list` 做前缀。
   - 统计值用 `count` 做前缀。
   - 插入用 `save` 或 `insert`。
   - 删除用 `remove` 或 `delete`。
   - 修改用 `update`。

2. [cite_start]**Boolean 字段修正** [cite: 33]：
   - 检查所有的 POJO 类，布尔类型的变量名**禁止**加 `is` 前缀（例如：将 `isDeleted` 改为 `deleted`），防止序列化错误。

3. [cite_start]**包装数据类型** [cite: 207-208]：
   - 确保所有的 POJO 类属性和 RPC/Service 方法的返回值使用**包装数据类型**（如 `Integer`），而不是基本数据类型（如 `int`），以避免 NPE 风险和默认值问题。

请扫描我的代码并自动应用这些命名和类型规范的更改。